# Scripts aposentados / one-shot

| Pasta | Conteúdo |
|-------|----------|
| `used/` | Migrações genéricas (slugify, romanos, parse-index, …) |
| `used/lde/` | Experimentos só LDE (indent por página, HTML antigo, …) |

Pipeline ativo: `scripts/` e `scripts/lde/`.
